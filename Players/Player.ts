/* Player class */
export class Player {
    private sid: number;
    public init: any;
    
    constructor(sid: number) {
        this.sid = sid;

        // INIT:
        this.init = function(/* idk i forgot args for this ill check later */) {

        }
    }
}